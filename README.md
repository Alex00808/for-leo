# Für Leo, mit Liebe

Open `index.html` in any modern browser. No build step is required.

The site is intentionally photo-free so it works immediately. To personalize it, edit the copy inside `index.html`, especially the three cards under `memory-track` and the three cards under `wish-grid`.

The ambient sound is generated by the browser and begins only after the opening button is clicked.
